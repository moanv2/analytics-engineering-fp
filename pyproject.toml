[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "analytics-engineering-fp"
version = "1.0.0"
description = "Open-Meteo weather analytics: dbt + DuckDB pipeline with a Streamlit dashboard"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
    "duckdb>=0.10.2",
    "certifi",
    "pandas",
    "pyarrow",
    "streamlit>=1.37",
    "plotly>=5.18",
    "tzdata",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "ruff",
    "sqlfluff>=3.0",
    "sqlfluff-templater-dbt",
    "pre-commit",
]

[tool.uv]
package = false

# This is an application, not an importable library: install dependencies only,
# no package auto-discovery (avoids setuptools flat-layout discovery errors).
[tool.setuptools]
py-modules = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["tests_python"]
