target/
dbt_packages/
logs/
# Ignore everything in data/ (e.g. the built DuckDB file) but keep the raw CSVs
data/*
!data/raw/
data/raw/*
!data/raw/open_meteo/
data/raw/open_meteo/*
!data/raw/open_meteo/*.csv
# Prebuilt DuckDB committed so the Streamlit Cloud demo runs without a build step
!data/weather_analytics.duckdb
.user.yml
package-lock.yml
__pycache__/
*.pyc
.claude/
dbt_internal_packages/
.gstack/

*.egg-info/
.venv/
